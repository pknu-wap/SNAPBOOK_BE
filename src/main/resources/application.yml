spring:
  application:
    name: easybooking
  datasource:
    url: jdbc:mysql://localhost:3306/easybooking?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&characterEncoding=utf8
    driverClassName: com.mysql.cj.jdbc.Driver
    username: root
    password: qwer1234
  jpa:
    hibernate:
      ddl-auto: update   # 개발용: 스키마 자동 반영. 운영에서는 validate 권장
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect